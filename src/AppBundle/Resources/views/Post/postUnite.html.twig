{% block body %}
    <h1>Create Page Form</h1>
    {%  if (form is not null) %}
        <form action="{{ url('post_unite') }}" method="POST" {{ form_enctype(form) }}>
            {{ form_widget(form) }}
            <input type="submit" value="submit">
        </form>
    {% endif %}
{% endblock body %}